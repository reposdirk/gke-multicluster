apiVersion: v1
kind: Service
metadata:
  name: nginx-demo-service
  namespace: default
  labels:
    app: nginx-demo
spec:
  ports:
  - port: 80
    targetPort: 80
    name: http
  selector:
    app: nginx-demo
  type: ClusterIP